// This executes on the web server.
// Set this as the startup project.

using Envivo.Fresnel.Bootstrap.AspNet;
using Envivo.Fresnel.Sample.Features.Model.A_Objects.Basics;

var domainModelType = typeof(BasicObject);

var app =
    new ServerApplicationBuilder()
    .WithModelAssembly(domainModelType.Assembly)
    .Build(args);

await app.RunAsync();
